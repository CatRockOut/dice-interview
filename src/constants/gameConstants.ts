export const GAME_CONSTANTS = {
    MIN_THRESHOLD: 0,
    MAX_THRESHOLD: 100,
    MAX_HISTORY_SIZE: 10,
    SLIDER_STEP: 20,
} as const;

export const COLORS = {
    WIN: '#2E7D32',
    LOSE: '#C62828',
    WIN_TEXT: '#1B5E20',
    BACKGROUND_LIGHT: 'rgba(0, 0, 0, .04)',
} as const;
